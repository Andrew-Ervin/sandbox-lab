import React, {useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Button} from '@/components/ui/button';
import {ManagedPiDesign} from './managed-pi';
import {ProjectSharing} from './project-sharing';
import './style.css';

const nodes = [
 {id:'chat',icon:'01',name:'Chat & orchestration',sub:'One trusted control layer',where:'Local host · React + FastAPI',life:'Available while the local launcher runs',body:'ChatKit presents conversations. FastAPI owns model requests, background jobs, file catalogs and execution routing. Switching conversations detaches the view, not the job. SQLite retains completed results; a backend restart interrupts in-flight work.',keep:'Conversations, job records and exported artifact versions in .local. This prototype uses one local owner, not production multi-user identity.'},
 {id:'quick',icon:'02',name:'Quick Python',sub:'Disposable execution',where:'lab-sandboxes · one pod per call',life:'30-second execution limit · reserve idles to zero after 5 minutes',body:'Small calculations and charts use a clean Python pod from a warm pool. It has a scientific baseline, no network and no Pi process. Each used pod is destroyed. A missing package or substantial project requires an explicit delegate_project call by the AI. Chat alone allocates no project.',keep:'Source, exported artifacts and a bounded local checkpoint of working files survive. The next call restores that conversation’s files; variables and installed packages do not survive.'},
 {id:'project',icon:'03',name:'AI project',sub:'Allocated only after a project handoff',where:'lab-agents · Coder workspace pod',life:'5-minute idle target · active jobs and previews protected',body:'A single clean, unassigned Coder reserve warms while a new chat is being routed. A delegated coding task or a project chat continuing existing developer source claims it; ordinary chats own no workspace. Used homes never return to the reserve. Coder provisions and connects the workspace. The local coder-native trial runs its coding harness in the Coder control plane and sends file/shell operations into a model-free workspace. The ori-pi engine remains the alternative. Analysis and app building share the project home; all chats in a project use that home.',keep:'Source, lockfiles, environments and caches survive on the project home PVC. Native Coder chat state stays in its control database. Stop removes compute; it does not delete the home.'},
 {id:'app',icon:'04',name:'Generated app',sub:'A process inside its project',where:'Port 3000 in the same workspace pod',life:'Stops when the process or workspace stops',body:'Building an app does not deploy a second hosting service. Its server runs inside the execution container; the native Coder trial keeps its model harness in the control plane. Opening the app starts it automatically from its saved recipe. The embedded preview forwards HTTP to it. Recent visible interaction renews the preview lease. An unattended or background panel does not keep it alive.',keep:'App source remains on the home PVC. Clicking the app resumes its workspace and replays its saved launch command without a chat message. Independent app hosting remains future work.'},
 {id:'dev',icon:'05',name:'Developer workstation',sub:'Human-owned, independent of chat',where:'lab-dev · Coder + code-server',life:'10-minute idle target · IDE/preview activity extends use',body:'The GUI is VS Code, files, terminal and upstream Pi Chat configured through Ori. It uses the same language tools as AI projects, plus the IDE. Pi Chat defaults to the right secondary sidebar. Sidebar messages use Pi RPC from the extension host; main-app conversations never execute in this human workspace.',keep:'A separate home PVC contains files, packages, sidebar history and Pi sessions. Sidebar turns follow the IDE extension-host lifetime; they are not the main app’s durable job queue.'},
 {id:'report',icon:'06',name:'Static app & artifacts',sub:'This field guide is an example',where:'Local artifact store → isolated preview',life:'No coding pod needed · preview listener expires after 2 idle minutes',body:'Self-contained HTML, PNGs and other exported files are immutable run artifacts. This interactive guide is included as a standalone HTML report in the repository; it can also be registered as a saved artifact app. Its preview listener can be recreated whenever you open it; no Coder workspace is allocated.',keep:'The saved file remains after its preview closes. Local artifact retention is manual today; execution scale-to-zero does not reclaim stored files.'},
];
const packages=[['Python','uv','pyproject.toml + uv.lock','.venv, ~/.cache/uv'],['JavaScript / TypeScript','npm','package.json + package-lock.json','node_modules, ~/.npm'],['C# / .NET','dotnet / NuGet','.csproj + packages.lock.json + global.json','~/.nuget, bin / obj'],['Julia','Pkg','Project.toml + Manifest.toml','~/.julia'],['Go','Go modules','go.mod + go.sum','~/go/pkg/mod, go-build'],['Rust','Cargo / rustup','Cargo.toml + Cargo.lock','~/.cargo, target'],['C / C++','CMake + chosen manager','CMake files + pinned deps / lockfile','build + manager cache'],['Shell','Bash / sh + user tools','scripts + versioned tool list','~/.local/bin']];
const stack=[['Interface','React, TypeScript, shadcn, ChatKit','Vite / vinext dev server on 3000; embedded apps and VS Code'],['Application','Python, FastAPI, ChatKit SDK, httpx','Single broker on 8787; SQLite WAL; bounded background jobs'],['Local infrastructure','Docker Desktop → kind → Kubernetes','Linux ARM64/AMD64; one local node; Calico network policies'],['Workspace control','Two Coder 2.36.4 installations','Separate human and AI identities, databases, namespaces and templates'],['Coding harness','Native Coder trial; Ori 0.14.1 → Pi 0.85.1 in GUI','Headless harness stays in its control plane; GUI uses Ori/Pi; Luna / xhigh'],['Developer GUI','code-server 4.106.3 + upstream Pi Chat 0.2.3','Pinned MIT extension; Pi RPC through ori-lab; persistent sessions'],['Model and downloads','OpenRouter + model gateway + package gateway','Provider key stays central; five-day package gate; internal-only DNS'],['Storage and previews','Home PVCs + local artifacts + SQLite','Separate loopback preview origins; HTTP forwarding; static file listeners']];
const links=[['OneDrive app folders','https://learn.microsoft.com/en-us/graph/onedrive-sharepoint-appfolder'],['Graph change tracking','https://learn.microsoft.com/en-us/graph/api/driveitem-delta?view=graph-rest-1.0'],['Conflict-safe uploads','https://learn.microsoft.com/en-us/graph/api/driveitem-createuploadsession?view=graph-rest-1.0'],['OneLake access','https://learn.microsoft.com/en-us/fabric/onelake/onelake-access-api'],['AKS system pools','https://learn.microsoft.com/en-us/azure/aks/use-system-pools'],['AKS autoscaler','https://learn.microsoft.com/en-us/azure/aks/cluster-autoscaler-overview']];

function App(){
 const [selected,setSelected]=useState('project'),[tab,setTab]=useState('Overview'),[minutes,setMinutes]=useState(0),[active,setActive]=useState(false),[preview,setPreview]=useState(false),[storage,setStorage]=useState('Blob'),[operation,setOperation]=useState('Build in sandbox');
 const node=nodes.find(n=>n.id===selected)!;
 const held=active||preview, limit=selected==='dev'?10:selected==='report'?2:5;
 const phase=selected==='chat'?'Control stays available':selected==='report'?(minutes>=2&&!preview?'Saved artifact · listener asleep':'Browser renders saved HTML'):selected==='quick'?(active?'Leased pod executing':minutes>=5?'Execution pool at zero':'Clean reserve ready'):held?'Workspace stays running':minutes>=limit?'Compute stopped · files retained':'Workspace running until idle deadline';
 return <main>
  <header><a className="brand" href="#" onClick={e=>{e.preventDefault();setTab('Overview')}}><span className="brand-mark">S</span> SANDBOX LAB <span className="slash">/</span> FIELD GUIDE</a><span className="tag">LOCAL IMPLEMENTATION · 07 SEP 2026</span></header>
  <section className="intro"><div><div className="eyebrow">A MAP OF YOUR EXECUTION PLATFORM</div><h1>One interface.<br/><em>Independent places to work.</em></h1><p>Follow a request from conversation to code, see what stays alive, and understand where every file and credential belongs.</p></div><div className="intro-stats"><div><strong>Auto</strong><span>routing in chat</span></div><div><strong>0</strong><span>idle execution pods*</span></div><div><strong>1 home</strong><span>per persistent workspace</span></div></div></section>
  <nav aria-label="Guide sections">{['Overview','Projects & sync','Lifecycle','Packages','Security','Managed Pi','Enterprise controls','Credentials','Storage options','Stack & AKS'].map(t=><Button className={tab===t?'nav active':'nav'} key={t} onClick={()=>setTab(t)} aria-pressed={tab===t}>{t}</Button>)}</nav>
  {tab==='Managed Pi'&&<ManagedPiDesign/>}
  {tab==='Overview'&&<>
   <div className="section-label"><span>01 / EXECUTION MAP</span><span>Select a card to inspect its boundary</span></div>
   <div className="overview"><section className="map" aria-label="Execution environments"><div className="zone"><span className="zone-label">CENTRAL · TRUSTED SERVICES</span><div className="central"><button className={selected==='chat'?'node selected':'node'} onClick={()=>setSelected('chat')}><span className="number">01</span><strong>Chat & orchestration</strong><small>React · FastAPI · SQLite</small></button><div className="service"><strong>Model gateway</strong><small>OpenRouter key stays here</small></div><div className="service"><strong>Coder controls</strong><small>Human + AI services</small></div></div></div><div className="connector">↓ Automatic routing and owner-scoped workspace selection ↓</div><div className="zone"><span className="zone-label">EXECUTION · SEPARATE PODS / SEPARATE HOMES</span><div className="execution">{nodes.slice(1,5).filter(n=>n.id!=='app').map(n=><button key={n.id} className={selected===n.id?'node selected':'node'} onClick={()=>setSelected(n.id)}><span className="number">{n.icon}</span><strong>{n.name}</strong><small>{n.sub}</small><span className="chip">{n.id==='quick'?'Single-use pod':n.id==='dev'?'VS Code + Pi':'Headless execution'}</span></button>)}</div><div className="nested"><button className={selected==='app'?'node selected':'node'} onClick={()=>setSelected('app')}><span className="number">04</span><strong>Generated app :3000</strong><small>Inside its AI or human workspace • no separate hosting container</small></button></div></div><div className="zone lower"><span className="zone-label">DURABLE FILES · SHORT-LIVED VIEWS</span><button className={selected==='report'?'node selected':'node'} onClick={()=>setSelected('report')}><span className="number">06</span><strong>Static apps & exported artifacts</strong><small>Per-run artifacts + per-chat quick checkpoint on local host • no execution pod</small></button></div></section><aside className="inspector"><div className="eyebrow">INSPECTING / {node.icon}</div><h2>{node.name}</h2><span className="where">{node.where}</span><p>{node.body}</p><div className="fact"><span>LIFETIME</span><p>{node.life}</p></div><div className="fact"><span>WHAT SURVIVES</span><p>{node.keep}</p></div><Button className="text-button" onClick={()=>setTab('Lifecycle')}>Explore idle behavior →</Button></aside></div>
   <div className="footnote">* Execution can reach zero. Your host, Linux runtime, kind node, Coder databases, gateways and chat services still run. This is a configuration map, not live telemetry.</div>
  </>}
  {tab==='Projects & sync'&&<ProjectSharing/>}
  {tab==='Lifecycle'&&<section className="deep"><div className="section-label">02 / LIFECYCLE SIMULATOR · ILLUSTRATIVE POLICY</div><div className="two"><div><h2>What happens when I walk away?</h2><p>Operational defaults now live in config/limits.env. docs/LIMITS.md catalogs capacity, file sizes, execution deadlines, dictation, deployment steps and retention. This simulator illustrates the defaults; it is not a live configuration reader.</p><p>Choose an environment and move the idle clock. A stopped project retains its files, but its running processes are gone.</p><label className="field">Environment<select value={selected} onChange={e=>setSelected(e.target.value)}>{nodes.map(n=><option key={n.id} value={n.id}>{n.name}</option>)}</select></label><label className="field">Minutes since last activity <b>{minutes} min</b><input type="range" min="0" max="45" step="1" value={minutes} onChange={e=>setMinutes(Number(e.target.value))}/></label><label className="check"><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/>An execution is actively running</label><label className="check"><input type="checkbox" checked={preview} onChange={e=>setPreview(e.target.checked)}/>Recent visible interaction in its preview / IDE</label></div><div className="state-card"><div className="eyebrow">{node.name.toUpperCase()}</div><h3 aria-live="polite">{phase}</h3><div className="timeline"><span style={{width:`${Math.min(100,minutes/45*100)}%`}}/></div><p>{node.keep}</p><small>Approximate thresholds, not a guarantee to the second. The reaper polls every 15 seconds. Real editor/terminal/Pi activity extends developer use. Background Coder traffic and status polling do not count. Capacity pressure can reclaim inactive AI compute earlier.</small></div></div><div className="three"><article><h3>App hosting today</h3><p>Source and the app server share one workspace container. In the native Coder trial its harness stays in the control plane; GUI Pi stays in the developer container. Port 3000 is proxied into an iframe. When that workspace stops, the server stops. Clicking the app resumes its home and replays .lab/app.json, then waits for port 3000. No chat or model request is sent.</p></article><article><h3>Closing a view</h3><p>Main-app jobs keep running when you navigate away. Preview leases expire after 120 seconds without renewal. The UI renews every 30 seconds only while visible with interaction in the past minute. Generated previews report pointer/key/scroll activity. VS Code reports edits, selections, terminal command starts and active Pi work; an idle connection is not activity.</p></article><article><h3>Different failure boundaries</h3><p>Copy to chat can create a conversation and headless home after source review. Opening ordinary chat or canceling that review does not allocate project compute. Pi dictation converts browser audio to mono 16 kHz PCM WAV before sending it through the model gateway (ZDR metadata checks apply when enabled); five minutes fits the 10 MB default. Audio is not saved locally.</p><p>Restarting the backend interrupts in-flight chat jobs; results already saved remain. Developer sidebar turns belong to the VS Code extension host and end if that process exits. Gateway expiry still applies; request-count quotas have been removed. They are not crash-resuming jobs.</p></article></div><div className="callout">For independently available apps on AKS: add a publish action that creates a separate static artifact deployment or app workload, with its own identity, resources and scaling policy. That deployment path is not implemented here.</div></section>}
  {tab==='Packages'&&<section className="deep"><div className="section-label">03 / REPRODUCIBLE PROJECTS</div><h2>Shared toolchains. Private dependencies.</h2><p className="lede">Projects have separate homes. Chats inside one project share its headless environment; the linked developer workstation has another independent home. Installing package X in one environment does not change another. Quick Python always starts fresh.</p><div className="table-wrap"><table><thead><tr>{['Language','Manager','Keep with source','Private writable state'].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{packages.map(r=><tr key={r[0]}>{r.map(c=><td key={c}>{c}</td>)}</tr>)}</tbody></table></div><div className="three"><article><h3>Image layer</h3><p>Linux libraries, SDKs and common scientific packages are centrally baked. Cached image layers speed startup without sharing a writable home. New OS packages require an image/template update; no sudo in workspaces.</p></article><article><h3>Project layer</h3><p>Manifests and lockfiles define dependencies. uv, npm, NuGet, Julia Pkg, Go and Cargo restore them. C/C++ compilers are included; additional libraries need reviewed images or approved source. Conan/vcpkg feeds and SDK downloads are not enabled.</p></article><article><h3>Quick path</h3><p>Python + polars, NumPy, SciPy, scikit-learn and Plotly are ready. No package downloads. Missing imports require a project handoff. Working files and submitted source are retained locally and restored into a new clean pod. No live kernel or package environment is saved.</p></article></div><div className="two"><article><h3>Five-day package gate</h3><p>Python, npm, Cargo and NuGet use registry publication dates. Go and Julia quarantine content for five days from first observation. All valid package names are accepted on supported registry routes; transitive dependencies receive the same age checks. Existing installed packages remain usable.</p><p>Only the trusted Network &amp; packages screen can approve an exact version/hash age exception for 24 hours, with a reason. No exceptions are created automatically.</p></article><article><h3>Only approved routes</h3><p>Persistent code reaches its own Coder control, internal DNS and the read-only package gateway. Only GUI workstations can reach the model gateway in the native Coder trial. External DNS, direct internet, arbitrary proxy tunnels and package uploads are blocked. Shared public downloads are hash-checked and cached centrally.</p><p>Main chat and Ori/Pi can use Exa through OpenRouter’s server search tool. Main-chat answers retain native ChatKit citation sources. MCP and use-case allowlists are future work. Allowed model/search traffic still requires governance; this is not a complete DLP system.</p></article></div><div className="callout">Homes currently request 2 GiB; kind local-path does not enforce that as a hard disk quota. Native caches belong to their image, language ABI and CPU architecture. Rebuild dependencies for the actual AKS image and architecture.</div></section>}
  {tab==='Security'&&<section className="deep"><div className="section-label">04 / ENFORCED BOUNDARIES · LOCAL PROTOTYPE</div><h2>Code is untrusted. Boundaries need enforcement.</h2><p className="lede">Coder manages persistent workspaces. In the local native Coder trial, headless inference stays in its control plane and code has no model route or token. GUI workstations still use Ori/Pi and a central gateway for OpenRouter. Quick Python uses bare disposable Kubernetes pods, without Pi, Coder or model credentials. A separate clean Coder reserve carries no conversation data or model allowance until claimed. Both unused reserves drain compute after five idle minutes; the clean Coder standby home is retained for reuse.</p><div className="three"><article><h3>Execution isolation</h3><p>Separate namespaces and home volumes. Non-root users, dropped capabilities, seccomp, no privilege escalation, no host/Docker mounts and no Kubernetes service-account tokens. CPU, memory, process and time limits bound work. Containers share the Linux VM kernel; this is not microVM isolation.</p></article><article><h3>Network enforcement</h3><p>Calico defaults to deny. Quick Python has no network or DNS. Both persistent environments reach only their Coder control, internal DNS and package gateway. Only GUI workstations also reach the model gateway in the native Coder trial. Direct internet, external DNS and arbitrary proxy tunnels are blocked.</p></article><article><h3>Secrets and identity</h3><p>The real provider key lives in trusted control services. GUI Pi receives an expiring, model-scoped gateway capability. Its Linux user and package hooks can reuse it. Native headless code receives no model capability. The browser receives neither the provider key nor a workspace model token.</p></article></div><div className="two"><article><h3>Package policy is age based</h3><p>No package-name whitelist. Python/npm/Cargo/NuGet require five days since publication; Go/Julia wait five days from first observation. Missing age evidence fails closed. Exact version/hash exceptions require the human control screen, a reason and a 24-hour expiry. Existing downloaded packages remain usable.</p><p>Only fixed registry routes support reads; uploads and arbitrary destinations are blocked. Package metadata names still travel to public registries. Model prompts and Exa search also leave through the approved gateway. These are deliberate data paths, not a complete data-loss prevention system.</p></article><article><h3>Files and browser boundaries</h3><p>Quick working-file snapshots are local, per conversation, and automatically restored. At most 40 files, 8 MB each, 16 MB total; no hidden files, symlinks or installed environments. Exported artifacts retain immutable per-run versions. Input reads pin directories and files without following symlinks, check regular-file type and enforce limits against actual bytes even if files grow. No Blob or OneLake connection is configured.</p><p>Generated apps render on separate origins in sandboxed iframes; the proxy strips credentials and restricts remote assets. Human VS Code uses its own authenticated Coder origin. Local session cookies, origin/host checks, CSRF and ownership checks protect control endpoints. The main chat UI loads ChatKit from cdn.platform.openai.com and preconnects to that same host; browser connection metadata reaches the CDN. Pod egress restrictions do not make the browser UI offline. This standalone field guide needs no external runtime.</p></article></div><div className="callout">Production gap: one local owner today, trusted host administrator credentials for provisioning and live status reads, local files without app-level encryption or automatic retention, and no distributed job recovery. AKS needs Entra ownership, least-privilege operator identities, storage governance, durable audit and tested egress controls. Status polls never renew compute leases. Activity signals are cost controls, not a defense against a malicious workspace keeping itself busy.</div></section>}
  {tab==='Enterprise controls'&&<section className="deep"><div className="section-label">05 / ENTERPRISE PLAN · NOT CONNECTED TO PRODUCTION SYSTEMS</div><h2>Let people build. Govern the connections.</h2><p className="lede">Business users can work freely with approved data inside their sandbox. Enterprise reads and writes require a trusted broker outside that sandbox. Pi proposes operations; the destination service enforces policy.</p><div className="callout"><strong>Current limitation:</strong> GUI Pi and developer code share a Linux user and readable model capability; scripts can reuse it. Quick and native Coder headless code have no model credentials or route. A separated managed GUI harness remains a proposal.</div><div className="auth-flow"><span>Existing app / IDE<br/><small>Verified user + project</small></span><b>→</b><span>Trusted Pi harness<br/><small>No local code execution</small></span><b>→</b><span>Tool / MCP broker<br/><small>OPA + exact approvals</small></span><b>→</b><span>Code or system<br/><small>Scoped operation only</small></span></div><div className="two"><article><h3>Explore an access policy</h3><p>This is a policy illustration, not a live authorization decision. Every operation still requires verified identity, project grants and bounded parameters.</p><label className="field">Operation<select value={operation} onChange={e=>setOperation(e.target.value)}>{['Build in sandbox','Read approved data','Write to development','Write to production','Delete enterprise records','Call an LLM from generated code'].map(o=><option key={o}>{o}</option>)}</select></label></article><div className="state-card"><div className="eyebrow">PROPOSED ENTERPRISE BEHAVIOR</div><h3 aria-live="polite">{operation==='Build in sandbox'?'Run within project limits':operation==='Read approved data'?'Authorize a bounded read':operation==='Call an LLM from generated code'?'Deny direct model access':operation==='Write to development'?'Require an independent approval':'Require two independent approvals'}</h3><p>{operation==='Build in sandbox'?'No enterprise credentials. Packages come from the internal mirror. Keep recovery checkpoints outside the code’s deletion authority.':operation==='Read approved data'?'MCP checks user, project, data class, rows and columns. A read-only database transaction enforces the restriction.':operation==='Call an LLM from generated code'?'Quick and native Coder headless pods already receive neither a model token nor a route to the gateway. GUI workspaces remain the exception. AI application development needs a separate governed credential path.':'Review exact targets and before/after values in a trusted approval control associated with the request. Bind approval to parameters, policy and resource version; expire and consume it atomically before a transactional commit.'}</p><small>{operation==='Write to production'?'Production also needs a verified change record. Keep production writes disabled for the first pilot.':'Undefined policy, timeout, stale identity or unavailable approval service means deny.'}</small></div></div><div className="three"><article><h3>Enforced today</h3><p>Restricted pods, default-deny egress, separate homes, bounded quick snapshots and isolated previews. Model requests deny data collection; ZDR is temporarily off in config/models.env. AWS/OpenAI are preferred, with Azure excluded for LLM calls. There are no request-count capability quotas. Quick and native headless model routes are blocked.</p></article><article><h3>Needs the new broker</h3><p>A separated GUI harness, Entra project authorization, production MCP policy and approvals, durable audit, and independently hosted apps. The temporary local mock-email experiment is not a production integration; native Coder MCP is disabled.</p></article><article><h3>Policies still matter</h3><p>ZDR does not prevent disclosure. Approved prompts, search queries and package metadata can carry sensitive data. Classify projects, minimize payloads, use internal mirrors, protect backups and review exported/published apps.</p></article></div><div className="two"><article><h3>Pi approvals</h3><p>Pi supports extension-based confirmation flows. They are not administrator-locked security when the user controls the process, settings and files. Put enforcement at authenticated services and network boundaries; do not rely on a prompt or shell-command blocklist.</p></article><article><h3>New users and machines</h3><p>The source package excludes personal conversations, apps, files and credentials. It includes this reference report, Azure instructions and example policy tests. Setup selects pinned Mac/Linux ARM64 or AMD64 assets; Windows uses Ubuntu/WSL2. Full clean-machine Linux/WSL2 testing remains outstanding.</p></article></div><div className="sources"><a href="https://openrouter.ai/docs/guides/features/zdr" target="_blank" rel="noreferrer">OpenRouter ZDR ↗</a><a href="https://github.com/badlogic/pi-mono/tree/main/packages/coding-agent" target="_blank" rel="noreferrer">Pi SDK and extension model ↗</a><a href="https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization" target="_blank" rel="noreferrer">MCP authorization ↗</a></div></section>}
  {tab==='Credentials'&&<section className="deep"><div className="section-label">04 / THE CREDENTIAL BOUNDARY</div><h2>Preloaded access, without the provider key.</h2><p className="lede">Open the workstation from Sandbox Lab and Pi is ready to use. The actual OpenRouter key is held by trusted services. The developer’s Linux account receives a limited gateway capability.</p><div className="auth-flow"><span>VS Code sidebar<br/><small>No token in webview</small></span><b>→</b><span>Ori → Pi<br/><small>Reads workspace capability</small></span><b>→</b><span>Model gateway<br/><small>Validates + injects real key</small></span><b>→</b><span>OpenRouter<br/><small>Luna · xhigh</small></span></div><div className="two"><article><h3>What is actually protected</h3><p>The provider key is not placed in the workspace, browser or generated app. The gateway restricts audience, model, expiry and provider privacy settings. Operator policy currently denies data collection but temporarily disables ZDR. Enable OPENROUTER_REQUIRE_ZDR in config/models.env to enforce it again. Search remains a separately governed disclosure path. Request-count quotas have been removed; credentials remain time-scoped.</p><p>The Linux user can read their own gateway token. File mode 0600 prevents other users from reading it; it does not hide it from that same user. Package hooks and code running as that user have the same access.</p></article><article><h3>How sign-in works</h3><p>The main app configures Ori’s environment login and Pi’s gateway provider. No separate OpenRouter sign-in is needed. While a workstation is running and recently used, the trusted backend renews its one-hour capability five minutes before expiry. Pi resolves the token file on each request, and dictation reads it per recording. Sleeping workstations receive fresh setup when opened. Other harnesses may cache credentials and need a new terminal/session.</p><p>For AKS, replace the local owner with Entra identity, scoped workload credentials, audited budgets and a trusted gateway. The current token remains usable by code. To prohibit that, move the managed Pi harness into a trusted separate pod and replace its execution tools with broker RPCs. Remove model credentials and model routes from code pods. This separated GUI design is not implemented locally; native Coder headless execution already uses a separate control-plane harness.</p></article></div><div className="callout">Coder’s workspace agent provides connectivity and lifecycle reporting. Native Coder Agents are the headless coding harness in the local trial. GUI Pi is a different coding harness, launched through Ori. Project file sync does not share their credentials or processes.</div></section>}
  {tab==='Storage options'&&<section className="deep"><div className="section-label">05 / STORAGE PROPOSAL · NOT CONNECTED</div><h2>Working disk and durable storage have different jobs.</h2><div className="callout">Current choice: keep local persistence. Quick checkpoints live under .local/quick-checkpoints; source and exported results live under .local/artifacts. Blob, OneLake and OneDrive below remain design options, not connected services.</div><p className="lede">Keep compilers, package managers and app servers on a normal Linux working volume. Checkpoint source and artifacts to a durable store. Changing that store does not change where code executes.</p><div className="segmented" aria-label="Compare durable storage">{['Blob','OneDrive','OneLake'].map(s=><Button key={s} className={storage===s?'active':''} onClick={()=>setStorage(s)} aria-pressed={storage===s}>{s}</Button>)}</div><div className="storage-choice"><span className="eyebrow">{storage==='Blob'?'RECOMMENDED CORE FOR THIS PLAN':storage==='OneDrive'?'OPTIONAL USER-OWNED PROJECT SYNC':'SHARED ANALYTICAL DATA'}</span><h3>{storage==='Blob'?'App-managed checkpoints and artifacts':storage==='OneDrive'?'A project folder in each user’s drive':'Fabric-governed datasets beside projects'}</h3><p>{storage==='Blob'?'Use owner/project IDs, immutable revisions and a trusted storage broker. Keep source snapshots, manifests, data and exported apps; restore a working volume on demand. Our recommendation is Blob for core application persistence, with Git for collaborative source control.':storage==='OneDrive'?'A trusted Graph sync service could create Apps/Sandbox Lab/<project-id>. Checkpoint edited source while work proceeds and before stop. On resume, hydrate the latest committed revision and restore language dependencies. This is a proposed sync adapter, not a live filesystem mount.':'OneLake exposes Fabric data through ADLS/Blob-compatible interfaces, with API differences and Fabric-managed permissions. It is a good candidate for governed datasets used by analysis projects. Treating it as a developer home or package cache would need a separate validated design.'}</p></div><div className="steps">{[['1','Edit locally','Workspace PVC is the active working tree.'],['2','Checkpoint','Debounce changes; upload source, locks and selected artifacts.'],['3','Commit revision','Record hashes, remote IDs and version only after upload succeeds.'],['4','Stop compute','Retain pending data if sync fails; never claim it was saved.'],['5','Resume','Hydrate revision, restore packages, replay launch recipe on open.']].map(r=><article key={r[0]}><span>{r[0]}</span><strong>{r[1]}</strong><p>{r[2]}</p></article>)}</div><div className="two"><article><h3>OneDrive permissions & existing work</h3><p>App-folder scope limits access to that folder. Users can place existing work there to import it; arbitrary folders outside it require another approved access flow. Entra app sign-in alone does not automatically grant Graph file access.</p><p>A proposed broker keeps drive/folder IDs, change cursors and versions centrally. Track remote changes and detect edit conflicts; do not silently overwrite a user’s OneDrive edits. Users can edit or delete their files.</p></article><article><h3>What we would not sync</h3><p>Exclude node_modules, .venv, package caches, native build output, temporary files, sockets and credentials. Keep lockfiles and a runtime image reference so each language manager can rebuild its environment.</p><p>Use a single-writer lease, ETag checks and a committed manifest. Handle throttling, quota, consent loss and partial uploads. “Saved to OneDrive” must mean the remote revision is acknowledged, not merely queued.</p></article></div><div className="sources">{links.slice(0,4).map(([title,url])=><a href={url} key={url} target="_blank" rel="noreferrer">{title} ↗</a>)}</div></section>}
  {tab==='Stack & AKS'&&<section className="deep"><div className="section-label">06 / WHAT IS BUILT & WHAT COMES NEXT</div><h2>The implementation, layer by layer.</h2><div className="table-wrap"><table><thead><tr><th>Layer</th><th>Tools</th><th>Configuration</th></tr></thead><tbody>{stack.map(r=><tr key={r[0]}>{r.map(c=><td key={c}>{c}</td>)}</tr>)}</tbody></table></div><div className="two"><article><h3>Local capacity today</h3><p>Quick: 4 concurrent executions, up to 8 pods, clean reserve 2–4, zero after idle. AI: 2 concurrent turns, at most 4 running workspaces and 50 retained homes. Projects request 0.5 CPU / 512 MiB and are limited to 2 CPUs / 4 GiB.</p><p>Namespaces and network policy isolate workloads, but local pods share a Linux kernel inside Docker Desktop. This is not a microVM per user. No Firecracker architecture is in this plan.</p></article><article><h3>AKS migration draft</h3><p>Entra identity; PostgreSQL; durable queue and worker leases; owner-scoped Blob artifacts; authenticated preview routing. Retain separate human/AI controls. Use workload identities only in trusted services, native Coder headless inference or a separated harness, an audited model gateway and internal package mirrors. Approved MCP routes can come from an API Center catalog behind an enforcing broker.</p><p>Scale execution user pools to zero. Keep system/services capacity available. Queue demand must become schedulable pods for Cluster Autoscaler to react. Set explicit resource, quota and cost ceilings; “scale as needed” cannot mean unlimited.</p></article></div><div className="three"><article><h3>Warm intelligently</h3><p>Forecast weekday ramps, retain a clean ready reserve and refill from measured arrival rate × tail startup time plus burst allowance. Schedule prewarm before busy periods; allow reactive waking overnight.</p></article><article><h3>Drain deliberately</h3><p>Protect active job and preview leases. Checkpoint before stopping workspaces; retain storage independently. Retire only unclaimed warm pods. Never reuse a pod that has executed another tenant’s code.</p></article><article><h3>Measure before sizing</h3><p>Minute-average execution counts do not reveal arrival rate, idle allocations or memory. Collect queue age, runtime, readiness and resource percentiles before sizing AKS.</p></article></div><div className="sources">{links.slice(4).map(([title,url])=><a href={url} key={url} target="_blank" rel="noreferrer">{title} ↗</a>)}</div></section>}
  <section className="deep"><div className="section-label">PROJECT WORKFLOW</div><h2>Open your project and keep working.</h2><div className="three"><article><h3>Open in VS Code</h3><p>One click creates or resumes a developer workstation, copies eligible project source and opens that folder. Saved source changes synchronize both ways every 15 seconds while both sides run. Conflicts require a user choice. Chat keeps its separate headless sandbox; packages and credentials stay separate. Npm app previews restore missing dependencies from the lockfile through the package gateway, then cache unchanged installs.</p></article><article><h3>One focused pane</h3><p>Opening a preview collapses history, and opening history collapses the preview. A brief collapse preserves the IDE; reopening after an extended pause resolves the app again and wakes compute if needed.</p></article><article><h3>Explain on demand</h3><p>The general assistant can read curated configuration documentation and non-secret runtime settings when asked technical questions. Documentation reads allocate no project. Approval requests offer a readable excerpt, native decisions and formatted Preview and full JSON tabs. Embedded raster images can be shown; remote images and active content stay blocked.</p></article></div></section><section className="deep"><div className="section-label">RECOVERY &amp; ACTIVITY</div><h2>Chat and coding have separate lifetimes.</h2><div className="three"><article><h3>Live code-session status</h3><p>The strip below chat reads Coder status every five seconds. Stop coding interrupts that agent turn and stops headless compute to terminate shell children too. Files remain saved; previews close and can restart on their next open. The separate GUI stays running. A lost chat response does not imply Coder stopped; active or unconfirmed turns remain protected from idle shutdown.</p></article><article><h3>Recover without replay</h3><p>Read-only Coder requests reconnect with bounded retries. Completed coding stays completed if the final response or artifact transfer fails. Output collection retries independently; prompts and tool mutations are never blindly replayed. The local launcher also renews the scoped Kubernetes broker credential before expiry.</p></article><article><h3>Useful, bounded telemetry</h3><p>The expandable activity card shows tool names, arguments, outcomes, provider-supplied reasoning and token usage when available. It retains up to 200 recent messages and 24,000 rendered characters. These are private project records; detailed traces are excluded from routine status polls.</p></article></div></section><footer><span>SANDBOX LAB / ARCHITECTURE FIELD GUIDE</span><span>Local implementation + clearly marked proposals · Saved reference, not live telemetry</span></footer>
 </main>
}
createRoot(document.getElementById('root')!).render(<App/>);
